Other Python command-line tools that are in Debian:

* gdebi -- http://bazaar.launchpad.net/~gdebi-developers/gdebi/trunk/files
  Looks like the best example.

* pylint -- http://hg.logilab.org/pylint/file/2c9220f1ee1c
* ufw -- http://bazaar.launchpad.net/~jdstrand/ufw/trunk/files


TODO Instead of hard-coding all the METS stuff, have the UI generate web forms based on METS profiles (prototypes?).
     e.g., read METS.xml, then generate a form for editing that.
     Repository-wide mets profile.  Org profiles can override; collection ones can override those.
